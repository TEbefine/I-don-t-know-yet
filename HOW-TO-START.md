# Sap — My Space 🌿

ตอนนี้เหลือ **ไฟล์เดียว**: `index.html` (รวมทุกอย่างไว้ในนั้นแล้ว)
ไฟล์ `my-content.js` เก่า **ไม่ใช้แล้ว** ลบทิ้งได้เลย

## เปิดดู
ดับเบิลคลิก **`index.html`** → เปิดในเบราว์เซอร์
ถ้าเพิ่งแก้ ให้รีเฟรชแบบล้างแคช: **`Cmd + Shift + R`**

## แก้เรื่องราว / รูป / เพลง
เปิด `index.html` ด้วย text editor → หาบล็อกที่เขียนว่า **"✏️ EDIT YOUR STORY HERE"** ด้านบน
แก้ได้ในนั้นที่เดียว:

| อยากเพิ่ม | เขียนแบบนี้ |
|---|---|
| ข้อความ | `{ type:"text", body:"..." }` |
| ประโยคสำคัญ | `{ type:"quote", body:"...", by:"— me" }` |
| รูป | `{ type:"image", src:"photo.png", caption:"..." }` |
| เพลง | `{ type:"music", url:"spotify หรือ youtube link" }` |

ใส่คอมมา `,` ท้ายทุกบล็อก → เซฟ → รีเฟรช

### รูป (สำคัญ!)
- วางไฟล์รูปไว้ **โฟลเดอร์เดียวกับ index.html**
- เขียนชื่อไฟล์ให้ **ตรงตัวพิมพ์ใหญ่/เล็กเป๊ะ** เช่น `SAP-01.png` ≠ `sap-01.png`
  (บนคอมไม่เป็นไร แต่บน Vercel ตัวพิมพ์สำคัญมาก)

## ขึ้น Vercel
ง่ายมาก เพราะเป็นเว็บ static:
1. ไป https://vercel.com → New Project → **drag โฟลเดอร์นี้ทั้งโฟลเดอร์** เข้าไป
   (ต้องมี `index.html` + ไฟล์รูป `SAP-01.png` อยู่ด้วยกัน)
2. กด Deploy → ได้ลิงก์เลย
ไม่ต้องตั้งค่า build อะไร — Vercel เห็น index.html แล้วเสิร์ฟให้อัตโนมัติ

---
*SAP by Teera — a quiet home on the internet.*
